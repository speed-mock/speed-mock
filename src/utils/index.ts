// eslint-disable-next-line
export const isArray = (target: any): boolean => Array.isArray(target)

// eslint-disable-next-line
export const isString = (target: any): boolean => typeof target === 'string'
