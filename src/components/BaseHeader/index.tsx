import React from 'react'
import { Layout } from 'antd'

const { Header } = Layout
const BaseHeader: React.FC = () => {
  return <Header style={{ background: '#fff', padding: 0 }}>page 1</Header>
}

export default BaseHeader
